# goblinmode frontend

Next.js 14 app router scaffold. Run:

```
cd frontend
npm install
npm run dev
```

TODO:
- wire wagmi config with Monad testnet
- import ABIs from ../artifacts after `hardhat compile`
- build launch/buy/sell flows and the goblin badge inventory
